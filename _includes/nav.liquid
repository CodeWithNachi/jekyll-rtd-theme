<ul>
  {% for item in include.nav %}
    <li><a href="{{ item.url }}">{{ item.title }}</a></li>

    {% if item.subnav %}
      {% include nav.liquid nav=item.subnav %}
    {% endif %}
  {% endfor %}
</ul>
